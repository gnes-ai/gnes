!FFmpegPreprocessor
parameter:
  start_doc_id: 0
  random_doc_id: True
  duplicate_rm: True
  use_phash_weight: False
  phash_thresh: 5
  kwargs:
    s: "420*360"
    vsync: vfr
    vf: select=eq(pict_type\,I)
property:
  is_trained: true