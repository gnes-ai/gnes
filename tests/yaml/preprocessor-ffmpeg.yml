!FFmpegPreprocessor
parameter:
  frame_size: "192*168"
  duplicate_rm: True
  use_phash_weight: False
  phash_thresh: 5

  kwargs:
    vsync: vfr
    vf: select=eq(pict_type\,I)
gnes_config:
  is_trained: true