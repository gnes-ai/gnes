!BasePytorchEncoder
parameter:
#  model_dir: ${VGG_MODEL}
  model_dir: /ext_data/image_encoder
  model_name: vgg16
  layers:
    - features
    - avgpool
    - x.view(x.size(0), -1)
    - classifier[0]
property:
  is_trained: true
